export const environmentToBranchMapping: { [key: string]: string } = {
  sandbox: 'sandbox',
  staging: 'staging',
  prod: 'main',
}

export const appStageMapping: { [key: string]: string } = {
  sandbox: 'sandbox',
  staging: 'staging',
  prod: 'main',
}

export const branchNameMapping: { [key: string]: string } = {
  sandbox: 'SANDBOX',
  staging: 'STAGING',
  prod: 'PRODUCTION',
}

export const accountNumberMapping: { [key: string]: string } = {
  sandbox: '721921051657',
  staging: '394147191967',
  prod: '218419970704',
}
